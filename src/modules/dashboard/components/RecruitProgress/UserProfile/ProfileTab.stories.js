import React from 'react';
import ProfileTab from './ProfileTab';

export default {
  title: 'ProfileTab',
  component: ProfileTab,
};

const Template = (args) => <ProfileTab {...args} />;

export const ProfileTabStory = Template.bind({});

ProfileTabStory.args = {
  
};