import React from 'react';
import ProfileDropdown from '@/componentsforms/ProfileDropdown';

export default {
  title: 'ProfileDropdown',
  component: ProfileDropdown,
};

const Template = (args) => <ProfileDropdown {...args} />;

export const ProfileDropdownStory = Template.bind({});

ProfileDropdownStory.args = {

};
